import { View, Text } from 'react-native'
import React from 'react'

const CheckoutCart = ({navigation}) => {
  return (
    <View style={{
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    }}>
      <Text>CheckoutCart</Text>
    </View>
  )
}

export default CheckoutCart